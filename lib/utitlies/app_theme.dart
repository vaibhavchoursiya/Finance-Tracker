import 'package:flutter/material.dart';

class AppTheme {
  static const Color dark = Colors.black;
  static const Color light = Colors.white;
  static const Color primary = Color.fromARGB(255, 11, 10, 22);
  static const Color accent = Color(0xFF4F48EC);
  static const Color ternary = Colors.redAccent;
}
